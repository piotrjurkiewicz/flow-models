`convert`
*********

.. toctree::
   :maxdepth: 2

.. argparse::
   :ref: flow_models.convert.parser
   :prog: flow-models-convert

The `convert` tool can be used to convert flow records between supported formats (`nfcapd` format and CSV and binary formats flow described in `formats`).
